              <% if !Like.exists?(user_id:current_user.id,micropost_id:micropost.id) %>
              <%= link_to "お気に入り",likes_micropost_path(micropost),method: :post , class: 'btn btn-default btn-xs'%>
              <% else %>
              <%= link_to "お気に入り解除",likes_micropost_path(micropost),method: :delete , class: 'btn btn-danger btn-xs'%>
              <% end %>